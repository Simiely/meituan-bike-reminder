# 签名配置示例 —— 复制为 keystore.properties（该文件已被 .gitignore 忽略，不会入库）
# 或者用同名环境变量：KEYSTORE_FILE / KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD
#
# storeFile 为密钥库路径，相对于 app 模块目录（MeiTuanOneTap/app/）。
storeFile=../../release.keystore
storePassword=在此填写你的密钥库口令
keyAlias=在此填写别名
keyPassword=在此填写密钥口令
